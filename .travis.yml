language: node_js

cache:
  directories:
    - "node_modules"

install:
    - npm install && npm update

script:
    - npm prepublish

deploy:
    provider: npm
    email: ksilviu.marian@gmail.com
    api_key: $NPM_TOKEN
